# Environment
.env
.venv/
venv/
env/

# Sample raw data is checked in so fresh clones can explore the pipeline (~8 MB).
# If data grows significantly (>500 MB), consider re-adding these ignores
# and using Git LFS or a separate data download step instead.
#
# Uncomment these lines to stop tracking raw data:
# data/raw/*
# data/processed/*
# data/generated/*
# data/reviewed/*

# Vector database — always gitignored (binary, regeneratable).
# Run `python pipeline.py vectorize --target all` after cloning.
data/vectordb/
!data/vectordb/.gitkeep

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
*.egg

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
