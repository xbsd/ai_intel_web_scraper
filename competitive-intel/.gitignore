# Environment
.env
.venv/
venv/
env/

# Raw/processed data is gitignored — regenerate locally via the pipeline.
# The vectordb is also gitignored (binary, regeneratable).
data/raw/*
!data/raw/.gitkeep
data/processed/*
!data/processed/.gitkeep
data/generated/*
!data/generated/.gitkeep
data/reviewed/*
!data/reviewed/.gitkeep

# Vector database — always gitignored (binary, regeneratable).
# Run `python pipeline.py vectorize --target all` after cloning.
data/vectordb/
!data/vectordb/.gitkeep

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
*.egg

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
